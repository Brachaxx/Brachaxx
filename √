using System;

class Program
{
    static void Main()
    {
        Console.Write("Podaj liczbę: ");
        double number = Convert.ToDouble(Console.ReadLine());
        
        double sqrt = Math.Sqrt(number);
        
        Console.WriteLine("Pierwiastek kwadratowy z liczby wynosi: " + sqrt);
    }
}
